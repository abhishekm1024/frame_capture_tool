*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Google Services — must be provided per environment, never committed
app/google-services.json

# Generated files
**/build/
**/.cxx/

# Keystore files
*.jks
*.keystore

# Release-signing credentials (template file `keystore.properties.template` is
# tracked; the real one with real passwords must never be committed).
/keystore.properties
